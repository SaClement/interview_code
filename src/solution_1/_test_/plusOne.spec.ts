import { plusOne } from "../src/plusOne";

describe("plusOne",() => {
  test("plus one", () => {
    let numStr = [4, 3, 2, 1];
    let numStr2 = [1, 2, 9, 9, 9];
    // expect( plusOne(numStr)).toEqual([4, 3, 2, 2]);
    // expect( plusOne(numStr2)).toEqual([1, 3, 0, 0, 0]);
  })
})