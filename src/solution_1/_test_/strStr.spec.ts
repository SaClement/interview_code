import { strStrKMP } from "../src/strStr"

describe("strStr", () => {
  test("find index from haystack", () => {
    let haystack = 'hello',
        needle = 'll';
    // expect(strStrKMP(haystack, needle)).toBe(2)
  })
})