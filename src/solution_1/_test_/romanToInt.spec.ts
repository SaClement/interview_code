import { romanToInt } from '../src/romanToInt'
describe("romanToInt", () => {
  test("Roman to Integer", () => {
    let str1 = "VI";
    let str2 = "IV";
    let str3 = "XVI";

    // expect(romanToInt(str1)).toBe(6);
    // expect(romanToInt(str2)).toBe(4);
    // expect(romanToInt(str3)).toBe(16);
  })
})