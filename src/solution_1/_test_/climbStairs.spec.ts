import { climbStairs, recursionClimbStairs, matrixClimbStairs, normalClimbStairs } from "../src/climbStairs"

describe("climbStairs", () => {
  test("climb stairs", () => {
    let n1 = 5;
    let n2 = 12;

    // expect(climbStairs(n1)).toBe(8);
    // expect(matrixClimbStairs(n1)).toBe(8);
    // expect(normalClimbStairs(n1)).toBe(8);
    // expect(recursionClimbStairs(n1)).toBe(8);
    // expect(recursionClimbStairs(n2)).toBe(233);
  })
})